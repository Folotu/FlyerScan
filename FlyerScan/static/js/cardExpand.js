

// // Get all the card elements
// const cards = document.querySelectorAll('.card');

// // Loop through the card elements
// cards.forEach(card => {
//   // Add a click event listener to each card element
//   card.addEventListener('click', () => {
//     // Check if the card element has the 'expanded' class
//     if (card.classList.contains('expanded')) {
//       // If it does, remove the 'expanded' and 'overlay' classes
//       card.classList.remove('expanded');
//       document.body.classList.remove('overlay');
//     } else {
//       // If it doesn't, add the 'expanded' and 'overlay' classes
//       card.classList.add('expanded');
//       document.body.classList.add('overlay');

//       // Add a click event listener to the close button
//       card.querySelector('.close').addEventListener('click', () => {
//         // Remove the 'expanded' and 'overlay' classes
//         card.classList.remove('expanded');
//         document.body.classList.remove('overlay');
//       });
//     }
//   });
// });
